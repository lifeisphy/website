<h2>Tag: <%= tag %></h2>
<ul class="passage-list">
  <% posts.forEach(function(post) { 
  %>
    <li class="passage-item">
      <a href="<%= post.site_path %>" data-partial="true"><%= post.filename %></a> <div class="preview"><%- summaries[post.site_path] %></div>    
    </li>  
  <% }) %>
</ul>